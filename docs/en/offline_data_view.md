# Offline data view

`mp-data-view` is read-only. It can coexist with a camera preview because it
does not create Robot, camera, or policy resources. It is not recorded-state
motion, deployment handoff, trajectory replay, open-loop policy evaluation, or
real-robot evaluation.

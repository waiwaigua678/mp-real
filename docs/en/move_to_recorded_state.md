# Move to recorded state

The recorded-state flow targets only `observation.state`, validates declared
names/units/semantics, and requires an explicit plan hash before movement. A
Baseline records only the source reference and position protocol; it does not
authorize motion. See `hardware_validation_stage_9.md` before any execution.
